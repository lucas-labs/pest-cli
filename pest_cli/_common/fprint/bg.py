# ruff: noqa: E731

brand = lambda text: f'<bg-brand>{text}</bg-brand>'
sec = lambda text: f'<bg-sec>{text}</bg-sec>'
blue = lambda text: f'<bg-blue>{text}</bg-blue>'
green = lambda text: f'<bg-green>{text}</bg-green>'
red = lambda text: f'<bg-red>{text}</bg-red>'
yellow = lambda text: f'<bg-yellow>{text}</bg-yellow>'
purple = lambda text: f'<bg-purple>{text}</bg-purple>'
cyan = lambda text: f'<bg-cyan>{text}</bg-cyan>'
pink = lambda text: f'<bg-pink>{text}</bg-pink>'
white = lambda text: f'<bg-white>{text}</bg-white>'
black = lambda text: f'<bg-black>{text}</bg-black>'
noop = lambda text: text
